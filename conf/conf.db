bash    ([info]="bashrc, prompt, functions, aliases, etc")
git     ([info]="main configuration, aliases")
vim     ([info]="main configuration, colors" \
         [backup]="~/.vim/" \
         [install]="[[ -d ~/.vim ]] || mkdir ~/.vim")
mc      ([info]="main configuration, pannels" \
         [backup]="~/.config/mc" \
         [install]="[[ -d ~/.config/mc ]] || mkdir ~/.config/mc")
tmux    ([info]="main configuration")
